
/***METODOS DE GENERACION, REENPLAZO Y SEPARACION   * /
 */
=======
/****** 
/**METODOS DE GENERACION, REEMPLAZO Y SEPARACION */

var mensaje = "Estoy aprendiento JavaScript";
var mensaje2 = "y programacion"
var total = 123456;

var resultado;
 
// repeat
// resultado = mensaje.repeat(5) ->repite un rexto determinado cantidad de veces

// replace -> no afecta el valor original cambiado 
// resultado = mensaje.replace("JavaScript", "a programar") ->busca la palabra JavaScript en el texto y lo cambia por "a programar"

// slice
// resultado = mensaje.slice(6) -> comienza el texto en el caracter selecionado 
// resultado = mensaje.slice(6, mensaje,length-5) -> inicio y final a mostrar en un texto

// split
// resultado = mensaje.split(" ") -> separa la cadena de texto en un arreglo

// trim
// resultado = mensaje.trim() -> elimina los espacios en blanco antes y despues de la cadena de texto

//resultado = total.toString(); -> convierte numeros en una cadena de texto

//resultado = mensaje.toLowerCase(); -> convierte todo el texto en minuscula

//resultado = mensaje.toUpperCase(); ->convierte todo el texto en mayuscula

//resultado = mensaje.concat(mensaje2, "y tengo ideas"); ->une las cadenas de texto que se pongan en el metodo




console.log(resultado);
